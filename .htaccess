# DBS401 - Group 02 - Apache Security Config
# /var/www/html/dbs401-oracle-app/.htaccess

# ── Disable directory browsing ────────────────────────────
Options -Indexes

# ── Protect sensitive files ───────────────────────────────
<FilesMatch "^(config\.php|ANSWER_KEY\.md|ORACLE_PAYLOAD_CHEATSHEET\.md|\.env)$">
    Require all denied
</FilesMatch>

# ── Protect lab-only tools/docs from direct web access ─────
RedirectMatch 403 ^/dbs401-oracle-app/docs/(ANSWER_KEY|ORACLE_PAYLOAD_CHEATSHEET).*
RedirectMatch 403 ^/dbs401-oracle-app/tools/(exploit|decode|verify).*

# ── Protect database folder ───────────────────────────────
<FilesMatch "\.(sql)$">
    Require all denied
</FilesMatch>

# ── Block direct access to inc_ files ────────────────────
<FilesMatch "^inc_">
    Require all denied
</FilesMatch>

# ── PHP settings ──────────────────────────────────────────
# Hide PHP version in headers
<IfModule mod_headers.c>
    Header unset X-Powered-By
    Header always set X-Content-Type-Options "nosniff"
    Header always set X-Frame-Options "SAMEORIGIN"
    Header always set X-XSS-Protection "1; mode=block"
</IfModule>

# ── Default charset ───────────────────────────────────────
AddDefaultCharset UTF-8

# ── DirectoryIndex ────────────────────────────────────────
DirectoryIndex index.php
